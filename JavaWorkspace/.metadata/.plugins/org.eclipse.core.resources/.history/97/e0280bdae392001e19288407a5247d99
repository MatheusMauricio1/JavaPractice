package essentials;

public class User {
	String name;
	String email;
	
	@Override
	public boolean equals(Object obj) {
		
		if(obj instanceof User) {
		User other = (User) obj;
		return super.equals(obj); 
		}
		return false;
	}
	
}
